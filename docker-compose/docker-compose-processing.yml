version: "3"
services:
  adapter:
    image: ghcr.io/datagov-cz/nkod-adapter:main
    build: 
      context: ../adapter
    environment:
      MS_APPLICATION: ${MS_APPLICATION}
      MS_TENANT: ${MS_TENANT}
      MS_SECRET: ${MS_SECRET}
      MS_SITE: ${MS_SITE}
      MS_APPLICATION_LIST: ${MS_APPLICATION_LIST}
      MS_APPLICATION_DRIVE: ${MS_APPLICATION_DRIVE}
      MS_APPLICATION_DIRECTORY: ${MS_APPLICATION_DIRECTORY}
      MS_LIST_SUGGESTIONS: ${MS_LIST_SUGGESTIONS}
      ISDS_LOGIN: ${ISDS_LOGIN}
      ISDS_PASSWORD: ${ISDS_PASSWORD}
      ISDS_URL: ${ISDS_URL}
    restart: always
    volumes:
      - adapter-data:/data/
volumes:
  adapter-data:
    name: nkod-adapter-data
    external: True
