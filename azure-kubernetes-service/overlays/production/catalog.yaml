apiVersion: apps/v1
kind: Deployment
metadata:
  name: nkd-catalog-deployment
  namespace: nkd
  labels:
    app.kubernetes.io/name: catalog
spec:
  template:
    spec:
      tolerations:
        - key: dedicated
          operator: Equal
          value: user1
          effect: NoSchedule
      nodeSelector:
        agentpool: user1
      containers:
        - name: nkd-catalog
          resources:
            requests:
              memory: "512Mi"
            limits:
              memory: "1Gi"
