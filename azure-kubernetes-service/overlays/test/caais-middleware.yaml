apiVersion: apps/v1
kind: Deployment
metadata:
  name: nkd-caais-middleware-deployment
  namespace: nkd
  labels:
    app.kubernetes.io/name: caais-middleware
spec:
    spec:
      containers:
        - name: nkd-caais-middleware
          resources:
            requests:
              memory: "256Mi"
            limits:
              memory: "512Mi"
